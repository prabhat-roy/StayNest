global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.staynest.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.staynest.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
