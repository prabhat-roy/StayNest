apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: staynest-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [staynest] }
  timeOffset: "+500ms"
  duration: 3m
