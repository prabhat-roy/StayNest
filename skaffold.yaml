# Skaffold pipeline for StayNest
apiVersion: skaffold/v4beta11
kind: Config
metadata: { name: staynest }
build:
  artifacts:
    - { image: staynest/api-gateway, context: src/api-gateway }
    - { image: staynest/identity-service, context: src/identity-service }
  local: { push: false }
deploy:
  helm:
    releases:
      - { name: api-gateway, chartPath: helm/charts/api-gateway, namespace: staynest }
profiles:
  - name: prod
    deploy: { kubectl: { manifests: ["manifests/prod/*.yaml"] } }
