# Sourcegraph (OSS) — universal code search across StayNest
image: { repository: sourcegraph/server, tag: 5.5.0 }
sourcegraph:
  externalURL: https://sourcegraph.staynest.internal
  repositoriesToAdd: [github.com/prabhat-roy/StayNest]
ingress: { enabled: true, hosts: [{ host: sourcegraph.staynest.internal }] }
