staynest:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres.staynest.svc
      user: dbt
      password: "{{ env_var('DBT_PASSWORD') }}"
      port: 5432
      dbname: staynest_warehouse
      schema: public
      threads: 8
    prod:
      type: trino
      host: trino.staynest.svc
      port: 8080
      catalog: iceberg
      schema: staynest_mart
      threads: 16
