apiVersion: v1
kind: Service
metadata:
  name: {{ include "asset-service.fullname" . }}
  labels: {{- include "asset-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "asset-service.selectorLabels" . | nindent 4 }}
