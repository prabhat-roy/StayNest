apiVersion: v1
kind: Service
metadata:
  name: {{ include "guest-service.fullname" . }}
  labels: {{- include "guest-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "guest-service.selectorLabels" . | nindent 4 }}
