apiVersion: v1
kind: Service
metadata:
  name: {{ include "staff-sso.fullname" . }}
  labels: {{- include "staff-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "staff-sso.selectorLabels" . | nindent 4 }}
