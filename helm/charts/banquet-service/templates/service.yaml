apiVersion: v1
kind: Service
metadata:
  name: {{ include "banquet-service.fullname" . }}
  labels: {{- include "banquet-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "banquet-service.selectorLabels" . | nindent 4 }}
