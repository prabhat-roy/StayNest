apiVersion: v1
kind: Service
metadata:
  name: {{ include "tier-service.fullname" . }}
  labels: {{- include "tier-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "tier-service.selectorLabels" . | nindent 4 }}
