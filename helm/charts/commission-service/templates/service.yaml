apiVersion: v1
kind: Service
metadata:
  name: {{ include "commission-service.fullname" . }}
  labels: {{- include "commission-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "commission-service.selectorLabels" . | nindent 4 }}
