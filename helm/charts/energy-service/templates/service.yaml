apiVersion: v1
kind: Service
metadata:
  name: {{ include "energy-service.fullname" . }}
  labels: {{- include "energy-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "energy-service.selectorLabels" . | nindent 4 }}
