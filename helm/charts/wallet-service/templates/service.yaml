apiVersion: v1
kind: Service
metadata:
  name: {{ include "wallet-service.fullname" . }}
  labels: {{- include "wallet-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "wallet-service.selectorLabels" . | nindent 4 }}
