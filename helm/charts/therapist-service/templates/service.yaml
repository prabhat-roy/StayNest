apiVersion: v1
kind: Service
metadata:
  name: {{ include "therapist-service.fullname" . }}
  labels: {{- include "therapist-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "therapist-service.selectorLabels" . | nindent 4 }}
