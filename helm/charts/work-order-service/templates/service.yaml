apiVersion: v1
kind: Service
metadata:
  name: {{ include "work-order-service.fullname" . }}
  labels: {{- include "work-order-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "work-order-service.selectorLabels" . | nindent 4 }}
