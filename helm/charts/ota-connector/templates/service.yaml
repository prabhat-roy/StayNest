apiVersion: v1
kind: Service
metadata:
  name: {{ include "ota-connector.fullname" . }}
  labels: {{- include "ota-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ota-connector.selectorLabels" . | nindent 4 }}
