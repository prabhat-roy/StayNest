apiVersion: v1
kind: Service
metadata:
  name: {{ include "vendor-service.fullname" . }}
  labels: {{- include "vendor-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "vendor-service.selectorLabels" . | nindent 4 }}
