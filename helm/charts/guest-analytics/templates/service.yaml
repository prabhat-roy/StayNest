apiVersion: v1
kind: Service
metadata:
  name: {{ include "guest-analytics.fullname" . }}
  labels: {{- include "guest-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "guest-analytics.selectorLabels" . | nindent 4 }}
