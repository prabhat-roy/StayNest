apiVersion: v1
kind: Service
metadata:
  name: {{ include "attendance-service.fullname" . }}
  labels: {{- include "attendance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "attendance-service.selectorLabels" . | nindent 4 }}
