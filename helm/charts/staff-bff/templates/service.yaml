apiVersion: v1
kind: Service
metadata:
  name: {{ include "staff-bff.fullname" . }}
  labels: {{- include "staff-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "staff-bff.selectorLabels" . | nindent 4 }}
