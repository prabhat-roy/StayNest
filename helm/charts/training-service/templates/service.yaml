apiVersion: v1
kind: Service
metadata:
  name: {{ include "training-service.fullname" . }}
  labels: {{- include "training-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "training-service.selectorLabels" . | nindent 4 }}
