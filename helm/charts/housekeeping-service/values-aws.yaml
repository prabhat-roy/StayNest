cloud: { provider: aws, storageClass: gp3, ingressClass: alb }
serviceAccount: { annotations: { eks.amazonaws.com/role-arn: "arn:aws:iam::{{ .Values.aws.accountId }}:role/staynest-housekeeping-service" } }
envFrom: [{ secretRef: { name: housekeeping-service-aws-secrets } }]
