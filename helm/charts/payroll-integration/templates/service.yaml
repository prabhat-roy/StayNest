apiVersion: v1
kind: Service
metadata:
  name: {{ include "payroll-integration.fullname" . }}
  labels: {{- include "payroll-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "payroll-integration.selectorLabels" . | nindent 4 }}
