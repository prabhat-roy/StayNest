apiVersion: v1
kind: Service
metadata:
  name: {{ include "channel-manager.fullname" . }}
  labels: {{- include "channel-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "channel-manager.selectorLabels" . | nindent 4 }}
