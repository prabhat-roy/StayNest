apiVersion: v1
kind: Service
metadata:
  name: {{ include "partnership-service.fullname" . }}
  labels: {{- include "partnership-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "partnership-service.selectorLabels" . | nindent 4 }}
