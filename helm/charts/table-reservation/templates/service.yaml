apiVersion: v1
kind: Service
metadata:
  name: {{ include "table-reservation.fullname" . }}
  labels: {{- include "table-reservation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "table-reservation.selectorLabels" . | nindent 4 }}
