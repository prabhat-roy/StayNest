apiVersion: v1
kind: Service
metadata:
  name: {{ include "av-service.fullname" . }}
  labels: {{- include "av-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "av-service.selectorLabels" . | nindent 4 }}
