apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipe-costing.fullname" . }}
  labels: {{- include "recipe-costing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "recipe-costing.selectorLabels" . | nindent 4 }}
