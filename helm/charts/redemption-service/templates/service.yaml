apiVersion: v1
kind: Service
metadata:
  name: {{ include "redemption-service.fullname" . }}
  labels: {{- include "redemption-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "redemption-service.selectorLabels" . | nindent 4 }}
