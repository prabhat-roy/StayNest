apiVersion: v1
kind: Service
metadata:
  name: {{ include "pms-service.fullname" . }}
  labels: {{- include "pms-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "pms-service.selectorLabels" . | nindent 4 }}
