apiVersion: v1
kind: Service
metadata:
  name: {{ include "inspection-service.fullname" . }}
  labels: {{- include "inspection-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "inspection-service.selectorLabels" . | nindent 4 }}
