apiVersion: v1
kind: Service
metadata:
  name: {{ include "pos-service.fullname" . }}
  labels: {{- include "pos-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pos-service.selectorLabels" . | nindent 4 }}
