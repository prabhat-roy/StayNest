apiVersion: v1
kind: Service
metadata:
  name: {{ include "pms-rbac.fullname" . }}
  labels: {{- include "pms-rbac.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pms-rbac.selectorLabels" . | nindent 4 }}
