apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-service.fullname" . }}
  labels: {{- include "retail-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "retail-service.selectorLabels" . | nindent 4 }}
