apiVersion: v1
kind: Service
metadata:
  name: {{ include "night-audit.fullname" . }}
  labels: {{- include "night-audit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "night-audit.selectorLabels" . | nindent 4 }}
