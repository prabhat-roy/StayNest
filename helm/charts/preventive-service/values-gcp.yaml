cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: staynest-preventive-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com
