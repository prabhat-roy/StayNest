apiVersion: v1
kind: Service
metadata:
  name: {{ include "preventive-service.fullname" . }}
  labels: {{- include "preventive-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "preventive-service.selectorLabels" . | nindent 4 }}
