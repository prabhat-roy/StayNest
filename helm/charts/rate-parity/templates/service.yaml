apiVersion: v1
kind: Service
metadata:
  name: {{ include "rate-parity.fullname" . }}
  labels: {{- include "rate-parity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rate-parity.selectorLabels" . | nindent 4 }}
