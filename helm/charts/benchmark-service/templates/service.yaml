apiVersion: v1
kind: Service
metadata:
  name: {{ include "benchmark-service.fullname" . }}
  labels: {{- include "benchmark-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "benchmark-service.selectorLabels" . | nindent 4 }}
