apiVersion: v1
kind: Service
metadata:
  name: {{ include "demand-forecast.fullname" . }}
  labels: {{- include "demand-forecast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "demand-forecast.selectorLabels" . | nindent 4 }}
