apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata: { name: staynest_python_service, title: New StayNest Python service, tags: [python, staynest] }
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Service info
      required: [name]
      properties:
        name: { title: Name, type: string }
  steps:
    - { id: fetch, name: Fetch, action: fetch:template, input: { url: ./skeletons/python, values: { name: "${{ parameters.name }}" } } }
