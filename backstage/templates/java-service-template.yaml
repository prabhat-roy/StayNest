apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata: { name: staynest_java_service, title: New StayNest Java/Spring service, tags: [java, staynest] }
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Service info
      required: [name]
      properties: { name: { title: Name, type: string } }
  steps:
    - { id: fetch, name: Fetch, action: fetch:template, input: { url: ./skeletons/java } }
