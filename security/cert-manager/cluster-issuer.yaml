apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: staynest-vault-issuer }
spec:
  vault:
    server: https://vault.staynest.internal
    path: pki/sign/staynest-mesh
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
        secretRef: { name: cert-manager-vault-token, key: token }
