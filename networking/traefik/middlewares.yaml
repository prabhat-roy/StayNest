apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata: { name: staynest-rate-limit, namespace: staynest }
spec: { rateLimit: { average: 100, burst: 200, sourceCriterion: { ipStrategy: { depth: 1 } } } }
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata: { name: staynest-headers, namespace: staynest }
spec:
  headers:
    customRequestHeaders: { X-Forwarded-Proto: https }
    contentSecurityPolicy: "default-src 'self'"
    frameDeny: true
    sslRedirect: true
    stsSeconds: 31536000
