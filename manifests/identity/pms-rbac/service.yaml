apiVersion: v1
kind: Service
metadata:
  name: pms-rbac
  namespace: staynest
  labels: { app.kubernetes.io/name: pms-rbac, staynest.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50024, targetPort: 50024 }]
  selector: { app.kubernetes.io/name: pms-rbac }
