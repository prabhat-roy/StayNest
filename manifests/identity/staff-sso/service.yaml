apiVersion: v1
kind: Service
metadata:
  name: staff-sso
  namespace: staynest
  labels: { app.kubernetes.io/name: staff-sso, staynest.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: staff-sso }
