apiVersion: apps/v1
kind: Deployment
metadata:
  name: staff-sso
  namespace: staynest
  labels:
    app.kubernetes.io/name: staff-sso
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: staff-sso } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: staff-sso, staynest.io/domain: identity }
    spec:
      serviceAccountName: staff-sso
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: staff-sso
          image: harbor.staynest.internal/staynest/staff-sso:latest
          ports: [{ containerPort: 50022, name: http }]
          envFrom: [{ configMapRef: { name: staff-sso } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
