apiVersion: v1
kind: Service
metadata:
  name: guest-sso
  namespace: staynest
  labels: { app.kubernetes.io/name: guest-sso, staynest.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50021, targetPort: 50021 }]
  selector: { app.kubernetes.io/name: guest-sso }
