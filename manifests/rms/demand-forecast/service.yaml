apiVersion: v1
kind: Service
metadata:
  name: demand-forecast
  namespace: staynest
  labels: { app.kubernetes.io/name: demand-forecast, staynest.io/domain: rms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: demand-forecast }
