apiVersion: v1
kind: Service
metadata:
  name: pricing-engine
  namespace: staynest
  labels: { app.kubernetes.io/name: pricing-engine, staynest.io/domain: rms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: pricing-engine }
