apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing-engine
  namespace: staynest
  labels:
    app.kubernetes.io/name: pricing-engine
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: rms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pricing-engine } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pricing-engine, staynest.io/domain: rms }
    spec:
      serviceAccountName: pricing-engine
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pricing-engine
          image: harbor.staynest.internal/staynest/pricing-engine:latest
          ports: [{ containerPort: 50081, name: http }]
          envFrom: [{ configMapRef: { name: pricing-engine } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
