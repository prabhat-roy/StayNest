apiVersion: apps/v1
kind: Deployment
metadata:
  name: pickup-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: pickup-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: rms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pickup-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pickup-service, staynest.io/domain: rms }
    spec:
      serviceAccountName: pickup-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pickup-service
          image: harbor.staynest.internal/staynest/pickup-service:latest
          ports: [{ containerPort: 50082, name: http }]
          envFrom: [{ configMapRef: { name: pickup-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
