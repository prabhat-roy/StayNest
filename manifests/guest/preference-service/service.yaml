apiVersion: v1
kind: Service
metadata:
  name: preference-service
  namespace: staynest
  labels: { app.kubernetes.io/name: preference-service, staynest.io/domain: guest }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: preference-service }
