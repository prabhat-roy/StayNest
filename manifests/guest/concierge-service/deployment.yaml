apiVersion: apps/v1
kind: Deployment
metadata:
  name: concierge-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: concierge-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: guest
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: concierge-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: concierge-service, staynest.io/domain: guest }
    spec:
      serviceAccountName: concierge-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: concierge-service
          image: harbor.staynest.internal/staynest/concierge-service:latest
          ports: [{ containerPort: 50122, name: http }]
          envFrom: [{ configMapRef: { name: concierge-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
