apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: chat-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: guest
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: chat-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: chat-service, staynest.io/domain: guest }
    spec:
      serviceAccountName: chat-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: chat-service
          image: harbor.staynest.internal/staynest/chat-service:latest
          ports: [{ containerPort: 50123, name: http }]
          envFrom: [{ configMapRef: { name: chat-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50123 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50123 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
