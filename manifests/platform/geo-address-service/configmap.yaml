apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: staynest
  labels: { app.kubernetes.io/name: geo-address-service, staynest.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
