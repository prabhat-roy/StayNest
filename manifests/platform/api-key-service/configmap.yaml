apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: staynest
  labels: { app.kubernetes.io/name: api-key-service, staynest.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
