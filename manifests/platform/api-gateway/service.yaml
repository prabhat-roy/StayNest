apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: staynest
  labels: { app.kubernetes.io/name: api-gateway, staynest.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50000, targetPort: 50000 }]
  selector: { app.kubernetes.io/name: api-gateway }
