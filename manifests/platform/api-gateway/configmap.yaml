apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway
  namespace: staynest
  labels: { app.kubernetes.io/name: api-gateway, staynest.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "50000"
