apiVersion: v1
kind: Service
metadata:
  name: outbox-relay
  namespace: staynest
  labels: { app.kubernetes.io/name: outbox-relay, staynest.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51002, targetPort: 51002 }]
  selector: { app.kubernetes.io/name: outbox-relay }
