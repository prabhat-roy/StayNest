apiVersion: apps/v1
kind: Deployment
metadata:
  name: kitchen-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: kitchen-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: fnb
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: kitchen-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: kitchen-service, staynest.io/domain: fnb }
    spec:
      serviceAccountName: kitchen-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: kitchen-service
          image: harbor.staynest.internal/staynest/kitchen-service:latest
          ports: [{ containerPort: 50163, name: http }]
          envFrom: [{ configMapRef: { name: kitchen-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
