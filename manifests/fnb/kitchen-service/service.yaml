apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
  namespace: staynest
  labels: { app.kubernetes.io/name: kitchen-service, staynest.io/domain: fnb }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: kitchen-service }
