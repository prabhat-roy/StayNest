apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: pos-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: fnb
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pos-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pos-service, staynest.io/domain: fnb }
    spec:
      serviceAccountName: pos-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pos-service
          image: harbor.staynest.internal/staynest/pos-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: pos-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
