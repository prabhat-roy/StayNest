apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: menu-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: fnb
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: menu-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: menu-service, staynest.io/domain: fnb }
    spec:
      serviceAccountName: menu-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: menu-service
          image: harbor.staynest.internal/staynest/menu-service:latest
          ports: [{ containerPort: 50161, name: http }]
          envFrom: [{ configMapRef: { name: menu-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
