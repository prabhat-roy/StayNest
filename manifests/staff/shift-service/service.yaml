apiVersion: v1
kind: Service
metadata:
  name: shift-service
  namespace: staynest
  labels: { app.kubernetes.io/name: shift-service, staynest.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: shift-service }
