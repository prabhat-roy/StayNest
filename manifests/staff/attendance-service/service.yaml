apiVersion: v1
kind: Service
metadata:
  name: attendance-service
  namespace: staynest
  labels: { app.kubernetes.io/name: attendance-service, staynest.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: attendance-service }
