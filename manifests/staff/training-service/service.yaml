apiVersion: v1
kind: Service
metadata:
  name: training-service
  namespace: staynest
  labels: { app.kubernetes.io/name: training-service, staynest.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50283, targetPort: 50283 }]
  selector: { app.kubernetes.io/name: training-service }
