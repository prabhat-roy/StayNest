apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: training-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: staff
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: training-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: training-service, staynest.io/domain: staff }
    spec:
      serviceAccountName: training-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: training-service
          image: harbor.staynest.internal/staynest/training-service:latest
          ports: [{ containerPort: 50283, name: http }]
          envFrom: [{ configMapRef: { name: training-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50283 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50283 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
