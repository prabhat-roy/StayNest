apiVersion: v1
kind: Service
metadata:
  name: payroll-integration
  namespace: staynest
  labels: { app.kubernetes.io/name: payroll-integration, staynest.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: payroll-integration }
