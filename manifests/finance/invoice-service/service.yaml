apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: staynest
  labels: { app.kubernetes.io/name: invoice-service, staynest.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: invoice-service }
