apiVersion: v1
kind: Service
metadata:
  name: folio-service
  namespace: staynest
  labels: { app.kubernetes.io/name: folio-service, staynest.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50244, targetPort: 50244 }]
  selector: { app.kubernetes.io/name: folio-service }
