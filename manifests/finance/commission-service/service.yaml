apiVersion: v1
kind: Service
metadata:
  name: commission-service
  namespace: staynest
  labels: { app.kubernetes.io/name: commission-service, staynest.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50243, targetPort: 50243 }]
  selector: { app.kubernetes.io/name: commission-service }
