apiVersion: apps/v1
kind: Deployment
metadata:
  name: preventive-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: preventive-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: maintenance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: preventive-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: preventive-service, staynest.io/domain: maintenance }
    spec:
      serviceAccountName: preventive-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: preventive-service
          image: harbor.staynest.internal/staynest/preventive-service:latest
          ports: [{ containerPort: 50302, name: http }]
          envFrom: [{ configMapRef: { name: preventive-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50302 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50302 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
