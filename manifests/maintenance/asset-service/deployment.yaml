apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: asset-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: maintenance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: asset-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: asset-service, staynest.io/domain: maintenance }
    spec:
      serviceAccountName: asset-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: asset-service
          image: harbor.staynest.internal/staynest/asset-service:latest
          ports: [{ containerPort: 50301, name: http }]
          envFrom: [{ configMapRef: { name: asset-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
