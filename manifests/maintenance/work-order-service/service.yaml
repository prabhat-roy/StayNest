apiVersion: v1
kind: Service
metadata:
  name: work-order-service
  namespace: staynest
  labels: { app.kubernetes.io/name: work-order-service, staynest.io/domain: maintenance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: work-order-service }
