apiVersion: apps/v1
kind: Deployment
metadata:
  name: av-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: av-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: event
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: av-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: av-service, staynest.io/domain: event }
    spec:
      serviceAccountName: av-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: av-service
          image: harbor.staynest.internal/staynest/av-service:latest
          ports: [{ containerPort: 50201, name: http }]
          envFrom: [{ configMapRef: { name: av-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
