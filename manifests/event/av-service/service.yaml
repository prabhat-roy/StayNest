apiVersion: v1
kind: Service
metadata:
  name: av-service
  namespace: staynest
  labels: { app.kubernetes.io/name: av-service, staynest.io/domain: event }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: av-service }
