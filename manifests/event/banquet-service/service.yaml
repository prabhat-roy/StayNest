apiVersion: v1
kind: Service
metadata:
  name: banquet-service
  namespace: staynest
  labels: { app.kubernetes.io/name: banquet-service, staynest.io/domain: event }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: banquet-service }
