apiVersion: apps/v1
kind: Deployment
metadata:
  name: banquet-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: banquet-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: event
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: banquet-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: banquet-service, staynest.io/domain: event }
    spec:
      serviceAccountName: banquet-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: banquet-service
          image: harbor.staynest.internal/staynest/banquet-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: banquet-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
