apiVersion: v1
kind: Service
metadata:
  name: contract-service
  namespace: staynest
  labels: { app.kubernetes.io/name: contract-service, staynest.io/domain: event }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: contract-service }
