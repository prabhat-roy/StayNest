apiVersion: v1
kind: Service
metadata:
  name: catering-service
  namespace: staynest
  labels: { app.kubernetes.io/name: catering-service, staynest.io/domain: event }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: catering-service }
