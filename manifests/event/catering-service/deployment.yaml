apiVersion: apps/v1
kind: Deployment
metadata:
  name: catering-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: catering-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: event
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: catering-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: catering-service, staynest.io/domain: event }
    spec:
      serviceAccountName: catering-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: catering-service
          image: harbor.staynest.internal/staynest/catering-service:latest
          ports: [{ containerPort: 50202, name: http }]
          envFrom: [{ configMapRef: { name: catering-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50202 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50202 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
