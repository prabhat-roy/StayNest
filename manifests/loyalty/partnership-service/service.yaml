apiVersion: v1
kind: Service
metadata:
  name: partnership-service
  namespace: staynest
  labels: { app.kubernetes.io/name: partnership-service, staynest.io/domain: loyalty }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: partnership-service }
