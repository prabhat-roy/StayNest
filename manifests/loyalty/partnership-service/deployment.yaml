apiVersion: apps/v1
kind: Deployment
metadata:
  name: partnership-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: partnership-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: loyalty
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: partnership-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: partnership-service, staynest.io/domain: loyalty }
    spec:
      serviceAccountName: partnership-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: partnership-service
          image: harbor.staynest.internal/staynest/partnership-service:latest
          ports: [{ containerPort: 50222, name: http }]
          envFrom: [{ configMapRef: { name: partnership-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
