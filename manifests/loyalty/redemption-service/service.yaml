apiVersion: v1
kind: Service
metadata:
  name: redemption-service
  namespace: staynest
  labels: { app.kubernetes.io/name: redemption-service, staynest.io/domain: loyalty }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: redemption-service }
