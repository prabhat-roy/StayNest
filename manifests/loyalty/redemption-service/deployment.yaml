apiVersion: apps/v1
kind: Deployment
metadata:
  name: redemption-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: redemption-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: loyalty
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: redemption-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: redemption-service, staynest.io/domain: loyalty }
    spec:
      serviceAccountName: redemption-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: redemption-service
          image: harbor.staynest.internal/staynest/redemption-service:latest
          ports: [{ containerPort: 50221, name: http }]
          envFrom: [{ configMapRef: { name: redemption-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
