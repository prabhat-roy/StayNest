apiVersion: v1
kind: Service
metadata:
  name: tier-service
  namespace: staynest
  labels: { app.kubernetes.io/name: tier-service, staynest.io/domain: loyalty }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: tier-service }
