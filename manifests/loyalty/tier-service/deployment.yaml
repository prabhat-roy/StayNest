apiVersion: apps/v1
kind: Deployment
metadata:
  name: tier-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: tier-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: loyalty
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: tier-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: tier-service, staynest.io/domain: loyalty }
    spec:
      serviceAccountName: tier-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: tier-service
          image: harbor.staynest.internal/staynest/tier-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: tier-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
