apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: wallet-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: loyalty
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: wallet-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: wallet-service, staynest.io/domain: loyalty }
    spec:
      serviceAccountName: wallet-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: wallet-service
          image: harbor.staynest.internal/staynest/wallet-service:latest
          ports: [{ containerPort: 50223, name: http }]
          envFrom: [{ configMapRef: { name: wallet-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
