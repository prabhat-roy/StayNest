apiVersion: v1
kind: Service
metadata:
  name: therapist-service
  namespace: staynest
  labels: { app.kubernetes.io/name: therapist-service, staynest.io/domain: spa }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50181, targetPort: 50181 }]
  selector: { app.kubernetes.io/name: therapist-service }
