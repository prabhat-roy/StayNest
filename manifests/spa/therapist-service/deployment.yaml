apiVersion: apps/v1
kind: Deployment
metadata:
  name: therapist-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: therapist-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: spa
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: therapist-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: therapist-service, staynest.io/domain: spa }
    spec:
      serviceAccountName: therapist-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: therapist-service
          image: harbor.staynest.internal/staynest/therapist-service:latest
          ports: [{ containerPort: 50181, name: http }]
          envFrom: [{ configMapRef: { name: therapist-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
