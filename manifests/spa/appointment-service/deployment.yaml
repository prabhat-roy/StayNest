apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: appointment-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: spa
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: appointment-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: appointment-service, staynest.io/domain: spa }
    spec:
      serviceAccountName: appointment-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: appointment-service
          image: harbor.staynest.internal/staynest/appointment-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: appointment-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
