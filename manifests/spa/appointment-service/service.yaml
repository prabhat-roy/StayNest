apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: staynest
  labels: { app.kubernetes.io/name: appointment-service, staynest.io/domain: spa }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: appointment-service }
