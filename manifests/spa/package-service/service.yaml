apiVersion: v1
kind: Service
metadata:
  name: package-service
  namespace: staynest
  labels: { app.kubernetes.io/name: package-service, staynest.io/domain: spa }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: package-service }
