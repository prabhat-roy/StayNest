apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: group-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: crs
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: group-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: group-service, staynest.io/domain: crs }
    spec:
      serviceAccountName: group-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: group-service
          image: harbor.staynest.internal/staynest/group-service:latest
          ports: [{ containerPort: 50062, name: http }]
          envFrom: [{ configMapRef: { name: group-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
