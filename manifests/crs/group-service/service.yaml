apiVersion: v1
kind: Service
metadata:
  name: group-service
  namespace: staynest
  labels: { app.kubernetes.io/name: group-service, staynest.io/domain: crs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: group-service }
