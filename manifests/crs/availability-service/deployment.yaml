apiVersion: apps/v1
kind: Deployment
metadata:
  name: availability-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: availability-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: crs
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: availability-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: availability-service, staynest.io/domain: crs }
    spec:
      serviceAccountName: availability-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: availability-service
          image: harbor.staynest.internal/staynest/availability-service:latest
          ports: [{ containerPort: 50060, name: http }]
          envFrom: [{ configMapRef: { name: availability-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
