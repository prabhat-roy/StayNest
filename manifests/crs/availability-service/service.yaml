apiVersion: v1
kind: Service
metadata:
  name: availability-service
  namespace: staynest
  labels: { app.kubernetes.io/name: availability-service, staynest.io/domain: crs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: availability-service }
