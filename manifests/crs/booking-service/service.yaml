apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: staynest
  labels: { app.kubernetes.io/name: booking-service, staynest.io/domain: crs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: booking-service }
