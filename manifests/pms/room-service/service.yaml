apiVersion: v1
kind: Service
metadata:
  name: room-service
  namespace: staynest
  labels: { app.kubernetes.io/name: room-service, staynest.io/domain: pms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50040, targetPort: 50040 }]
  selector: { app.kubernetes.io/name: room-service }
