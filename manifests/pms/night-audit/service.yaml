apiVersion: v1
kind: Service
metadata:
  name: night-audit
  namespace: staynest
  labels: { app.kubernetes.io/name: night-audit, staynest.io/domain: pms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50043, targetPort: 50043 }]
  selector: { app.kubernetes.io/name: night-audit }
