apiVersion: v1
kind: Service
metadata:
  name: rate-service
  namespace: staynest
  labels: { app.kubernetes.io/name: rate-service, staynest.io/domain: pms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: rate-service }
