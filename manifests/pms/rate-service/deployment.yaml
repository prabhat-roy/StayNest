apiVersion: apps/v1
kind: Deployment
metadata:
  name: rate-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: rate-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: pms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rate-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rate-service, staynest.io/domain: pms }
    spec:
      serviceAccountName: rate-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rate-service
          image: harbor.staynest.internal/staynest/rate-service:latest
          ports: [{ containerPort: 50041, name: http }]
          envFrom: [{ configMapRef: { name: rate-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
