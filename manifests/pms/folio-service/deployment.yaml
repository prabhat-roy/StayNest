apiVersion: apps/v1
kind: Deployment
metadata:
  name: folio-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: folio-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: pms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: folio-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: folio-service, staynest.io/domain: pms }
    spec:
      serviceAccountName: folio-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: folio-service
          image: harbor.staynest.internal/staynest/folio-service:latest
          ports: [{ containerPort: 50042, name: http }]
          envFrom: [{ configMapRef: { name: folio-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
