apiVersion: v1
kind: Service
metadata:
  name: occupancy-analytics
  namespace: staynest
  labels: { app.kubernetes.io/name: occupancy-analytics, staynest.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50321, targetPort: 50321 }]
  selector: { app.kubernetes.io/name: occupancy-analytics }
