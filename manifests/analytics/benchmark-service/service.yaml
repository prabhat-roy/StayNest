apiVersion: v1
kind: Service
metadata:
  name: benchmark-service
  namespace: staynest
  labels: { app.kubernetes.io/name: benchmark-service, staynest.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50323, targetPort: 50323 }]
  selector: { app.kubernetes.io/name: benchmark-service }
