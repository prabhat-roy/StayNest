apiVersion: v1
kind: Service
metadata:
  name: guest-analytics
  namespace: staynest
  labels: { app.kubernetes.io/name: guest-analytics, staynest.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50322, targetPort: 50322 }]
  selector: { app.kubernetes.io/name: guest-analytics }
