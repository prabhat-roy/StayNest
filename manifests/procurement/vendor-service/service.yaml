apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: staynest
  labels: { app.kubernetes.io/name: vendor-service, staynest.io/domain: procurement }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: vendor-service }
