apiVersion: v1
kind: Service
metadata:
  name: recipe-costing
  namespace: staynest
  labels: { app.kubernetes.io/name: recipe-costing, staynest.io/domain: procurement }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: recipe-costing }
