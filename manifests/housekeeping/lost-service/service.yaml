apiVersion: v1
kind: Service
metadata:
  name: lost-service
  namespace: staynest
  labels: { app.kubernetes.io/name: lost-service, staynest.io/domain: housekeeping }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50143, targetPort: 50143 }]
  selector: { app.kubernetes.io/name: lost-service }
