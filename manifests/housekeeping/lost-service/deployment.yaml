apiVersion: apps/v1
kind: Deployment
metadata:
  name: lost-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: lost-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: housekeeping
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: lost-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: lost-service, staynest.io/domain: housekeeping }
    spec:
      serviceAccountName: lost-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: lost-service
          image: harbor.staynest.internal/staynest/lost-service:latest
          ports: [{ containerPort: 50143, name: http }]
          envFrom: [{ configMapRef: { name: lost-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50143 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50143 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
