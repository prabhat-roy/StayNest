apiVersion: apps/v1
kind: Deployment
metadata:
  name: inspection-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: inspection-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: housekeeping
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: inspection-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: inspection-service, staynest.io/domain: housekeeping }
    spec:
      serviceAccountName: inspection-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: inspection-service
          image: harbor.staynest.internal/staynest/inspection-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: inspection-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
