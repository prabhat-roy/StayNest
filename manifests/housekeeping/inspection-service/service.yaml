apiVersion: v1
kind: Service
metadata:
  name: inspection-service
  namespace: staynest
  labels: { app.kubernetes.io/name: inspection-service, staynest.io/domain: housekeeping }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: inspection-service }
