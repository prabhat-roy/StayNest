apiVersion: v1
kind: Service
metadata:
  name: linen-service
  namespace: staynest
  labels: { app.kubernetes.io/name: linen-service, staynest.io/domain: housekeeping }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: linen-service }
