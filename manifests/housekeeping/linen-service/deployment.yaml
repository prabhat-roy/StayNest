apiVersion: apps/v1
kind: Deployment
metadata:
  name: linen-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: linen-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: housekeeping
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: linen-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: linen-service, staynest.io/domain: housekeeping }
    spec:
      serviceAccountName: linen-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: linen-service
          image: harbor.staynest.internal/staynest/linen-service:latest
          ports: [{ containerPort: 50142, name: http }]
          envFrom: [{ configMapRef: { name: linen-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
