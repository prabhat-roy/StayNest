apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: task-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: housekeeping
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: task-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: task-service, staynest.io/domain: housekeeping }
    spec:
      serviceAccountName: task-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: task-service
          image: harbor.staynest.internal/staynest/task-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: task-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
