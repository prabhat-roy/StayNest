apiVersion: v1
kind: Service
metadata:
  name: ota-connector
  namespace: staynest
  labels: { app.kubernetes.io/name: ota-connector, staynest.io/domain: channel-manager }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: ota-connector }
