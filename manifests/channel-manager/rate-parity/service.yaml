apiVersion: v1
kind: Service
metadata:
  name: rate-parity
  namespace: staynest
  labels: { app.kubernetes.io/name: rate-parity, staynest.io/domain: channel-manager }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: rate-parity }
