apiVersion: apps/v1
kind: Deployment
metadata:
  name: gds-service
  namespace: staynest
  labels:
    app.kubernetes.io/name: gds-service
    app.kubernetes.io/part-of: staynest
    staynest.io/domain: channel-manager
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: gds-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: gds-service, staynest.io/domain: channel-manager }
    spec:
      serviceAccountName: gds-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: gds-service
          image: harbor.staynest.internal/staynest/gds-service:latest
          ports: [{ containerPort: 50101, name: http }]
          envFrom: [{ configMapRef: { name: gds-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
