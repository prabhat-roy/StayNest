apiVersion: v1
kind: Service
metadata:
  name: gds-service
  namespace: staynest
  labels: { app.kubernetes.io/name: gds-service, staynest.io/domain: channel-manager }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: gds-service }
