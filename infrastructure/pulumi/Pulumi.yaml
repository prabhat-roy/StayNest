name: staynest
description: StayNest — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://staynest-pulumi-state
config:
  staynest:env: prod
  staynest:cloud: aws
